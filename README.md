# CHIP-8-emulator
A minimal CHIP-8 emulator written in Python — one file, no dependencies beyond pygame. Implements the full CHIP-8 instruction set (~35 opcodes), 64×32 display, hex keypad input, and 60Hz timers. Built as a learning project for understanding how emulators work.
